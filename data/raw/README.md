# Raw Data

Place the raw input CSV files for the neonatal benchmark here:

- `ydus_llm_scores.csv`
- `taxonomy_annotations.csv`

Local symlinks may also be used. The analysis scripts read these paths by default unless the environment variables `NEO_SCORES_CSV` or `NEO_TAXONOMY_CSV` are set.
